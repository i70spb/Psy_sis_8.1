---
title: "Методы"
level: "L1-L4"
status: "approved"
---

# Методы БО840

---

## Легенда

| Символ | Значение |
|--------|----------|
| `↑` | Рост / увеличение |
| `↓` | Падение / уменьшение |
| `→` | Трансформация или направление |
| `↑↑` / `↓↓` | Интенсивное изменение (>3 пунктов по шкале) |

**Связь с переменными:** все методы работают с `P_heat`, `α`, `St`, `ΔI`, `AG_flow`, `Som`, `T`.

---

## Содержание

1. `#метод_сброс_P_heat` — аварийное снижение перегрева
2. `#метод_микро_дозы` — постепенное введение нового опыта
3. `#метод_возвращение_авторства` — восстановление α
4. `#метод_выход_на_твёрдую_землю` — выход из болотного статуса
5. `#метод_разделение_дара_и_долга` — выход из долговой петли
6. `#метод_разрешение_быть_животным` — снятие телесных запретов
7. `#метод_стекло` — защита от вербальной агрессии
8. `#метод_эвакуация` — выход из системной травли
9. `#метод_чей_это_стыд` — разделение своего и чужого стыда
10. `#метод_разделение_совести_и_стыда` — отличие внутреннего сигнала от внешнего давления
11. `#метод_легализация_желания` — снятие запрета на желание
12. `#метод_внутренний_оппонент` — проверка убеждений
13. `#метод_протокол_пересмотра_нормы` — осознанное изменение старого правила
14. `#метод_чистый_выход` — завершение контакта без потери себя
15. `#метод_консолидация_или_исход` — стратегия выживания в токсичной среде
16. `#метод_калибровка_карты_мира` — сверка прогноза с реальностью
17. `#метод_проверка_пустоты` — проверка наличия намерения
18. `#метод_микро_открытие` — постепенный выход из маски
19. `#метод_выдержать_стыд` — интеграция телесного стыда
20. `#метод_разделение_мысли_и_действия` — преодоление разрыва между знанием и поведением

---

## 1. #метод_сброс_P_heat — Сброс перегрева

**Уровень:** L0–L1

**Для чего:** Аварийное снижение `P_heat` при > 8. Когда система не может мыслить, дышать, выбирать.

**Алгоритм:**
1. Остановиться. Прекратить любую когнитивную нагрузку.
2. Дыхание: выдох длиннее вдоха (4-6-8 или квадратное дыхание).
3. Телесный выход: если есть физическая энергия — дать ей выход (ходьба, напряжение мышц, встряска).
4. Переключение: простая сенсорная задача (назвать 5 предметов вокруг, ощутить 4 точки опоры).
5. Проверка: `P_heat` снизился до < 7? Если нет — повторить.

**Ожидаемый результат:** `P_heat` ↓ на 2–3 пункта. Доступ к `att/s` и возможность рефлексии.

**Ограничения:** Не заменяет терапию при хроническом `P_heat > 8`. При соматической травме — только с терапевтом.

**Связи (локальные):**
- `#переменная_P_heat` — целевая переменная
- `#аксиома_золотая_середина` — основание

---

## 2. #метод_микро_дозы — Микро-дозы

**Уровень:** L3

**Для чего:** Постепенное введение нового опыта маленькими шагами. Выход из `#ловушка_стеклянный_пол`, `#ловушка_вечный_студент`.

**Алгоритм:**
1. Разбить задачу на 10 шагов.
2. Сделать первый.
3. Похвалить себя.
4. Только потом — второй.
5. Фиксировать прогресс.

**Ожидаемый результат:** `AG_flow ↑`, `P_heat ↓`, `α ↑`.

**Связи (локальные):**
- `#ловушка_стеклянный_пол` — закрывает
- `#ловушка_вечный_студент` — закрывает
- `#переменная_AG_flow` — растёт

---

## 3. #метод_возвращение_авторства — Возвращение α

**Уровень:** L1–L3

**Для чего:** Восстановление способности выбирать. Выход из ловушек, где `α` низкое.

**Алгоритм:**
1. Найти одну микро-зону выбора.
2. Зафиксировать: «Это я выбрал(а) сам(а)».
3. Повторить с другой зоной.
4. Фиксировать каждый выбор.

**Связи (локальные):**
- `#ловушка_внутренний_надзиратель` — закрывает
- `#ловушка_жизнь_чужими_целями` — закрывает
- `#переменная_α` — целевая переменная

---

## 4. #метод_выход_на_твёрдую_землю — Выход на твёрдую землю

**Уровень:** L2–L3

**Для чего:** Выход из `#ловушка_царь_болотной_кочки`. Расширение зоны реальной компетенции вместо удержания власти над маленькой территорией.

**Алгоритм:**
1. Задать вопрос: «Что именно я контролирую и какова цена этого царства?»
2. Признать, что локальный статус не заменяет реальное авторство.
3. Начать расширять зону компетенции.
4. Постепенно отпускать контроль над маленьким болотом.

**Связи (локальные):**
- `#ловушка_царь_болотной_кочки` — закрывает
- `#переменная_α` — растёт
- `#ось_статус` — нормализуется

---

## 5. #метод_разделение_дара_и_долга — Разделение дара и долга

**Уровень:** L2

**Для чего:** Выход из `#ловушка_долговая_петля`. Признание полученного блага без превращения его в пожизненную кабалу.

**Алгоритм:**
1. Признать: «Я получил(а) благо».
2. Отделить: «Это не делает меня должником навсегда».
3. Зафиксировать: «Я могу быть благодарным(ой) и свободным(ой) одновременно».
4. Проверить: «Я отдаю из благодарности или из страха?»

**Связи (локальные):**
- `#ловушка_долговая_петля` — закрывает
- `#переменная_α` — растёт

---

## 6. #метод_разрешение_быть_животным — Разрешение быть животным

**Уровень:** L0–L1

**Для чего:** Выход из `#ловушка_принцессы_не_пукают` и других телесных запретов. Снять стыд за естественные проявления тела.

**Алгоритм:**
1. Заметить телесный сигнал (напряжение, ком в горле, спазм).
2. Назвать его: «Я чувствую напряжение в животе».
3. Снять запрет: «Я разрешаю себе чувствовать это».
4. Сделать простое телесное действие: вздохнуть, потянуться, выдохнуть.
5. Повторить в безопасной ситуации.

**Связи (локальные):**
- `#ловушка_принцессы_не_пукают` — закрывает
- `#переменная_Som` — снижается
- `#переменная_α` — растёт

---

## 7. #метод_стекло — Стекло

**Уровень:** L1

**Для чего:** Экстренная защита от вербальной агрессии. Защита границ без разрыва контакта.

**Алгоритм:**
1. В момент атаки представить толстое стекло между собой и нападающим.
2. Эмоции не передаются через стекло.
3. Ты видишь агрессию, но не впускаешь её внутрь.
4. Сохраняешь `α`, не переходишь в защиту или атаку.

**Связи (локальные):**
- `#ловушка_пустая_лодка` — защищает от проекции
- `#ловушка_внутренний_надзиратель` — защищает от самобичевания

---

## 8. #метод_эвакуация — Эвакуация

**Уровень:** L1

**Для чего:** Выход из системной травли, абьюза, фатальной игры.

**Алгоритм:**
1. Прекратить доказывать что-либо.
2. Собрать улики (если нужно).
3. Выйти физически и психологически.
4. При угрозе — обратиться к юристу или в службу поддержки.

**Связи (локальные):**
- `#ловушка_фатальная_игра` — закрывает
- `#ловушка_контроль_через_уход` — закрывает

---

## 9. #метод_чей_это_стыд — Чей это стыд?

**Уровень:** L2

**Для чего:** Разделить свой стыд и стыд, который перекладывают на тебя. Выход из `#ловушка_ложная_совесть`.

**Алгоритм:**
1. Меня обвиняют.
2. «Что я чувствую?»
3. «Это моя вина или мне пытаются переложить стыд?»
4. Если стыд чужой — не принимать.
5. Отвечать, но не извиняться за чужой стыд.

**Связи (локальные):**
- `#ловушка_ложная_совесть` — закрывает
- `#переменная_α` — растёт
- `#ось_статус` — растёт

---

## 10. #метод_разделение_совести_и_стыда — Разделение совести и стыда

**Уровень:** L3

**Для чего:** Отличить внутренний сигнал (`D_core`) от внешнего давления (`D_soc`). Выход из `#ловушка_ложная_совесть`.

**Алгоритм:**
1. «Это моё правило или чужое?»
2. Если чужое — это стыд, а не совесть.
3. Принять решение: подчиниться стыду или действовать по своей логике.
4. Зафиксировать Exp.

**Связи (локальные):**
- `#ловушка_ложная_совесть` — закрывает
- `#переменная_α` — растёт
- `#переменная_ΔI` — снижается

---

## 11. #метод_легализация_желания — Легализация желания

**Уровень:** L1–L3

**Для чего:** Выход из `#ловушка_моральный_шантаж_себя`. Назвать желание прямо и отделить его от оценки «хороший/плохой».

**Алгоритм:**
1. Поймать момент: «Мне стыдно хотеть этого».
2. Сказать: «Я хочу [назвать]».
3. Отделить желание от оценки: «Хотеть — не значит делать».
4. Принять: желание — это сигнал, а не приговор.

**Связи (локальные):**
- `#ловушка_моральный_шантаж_себя` — закрывает
- `#ловушка_принцессы_не_пукают` — закрывает

---

## 12. #метод_внутренний_оппонент — Внутренний оппонент

**Уровень:** L3

**Для чего:** Проверка убеждений. Найти слепые зоны и подтверждающие искажения.

**Алгоритм:**
1. Сформулировать позицию.
2. Найти 3 довода ЗА.
3. Найти 3 довода ПРОТИВ.
4. Если не можешь найти доводы против — ты ищешь подтверждения.

**Связи (локальные):**
- `#ловушка_слепая_зона` — закрывает
- `#ловушка_розовые_очки` — закрывает

---

## 13. #метод_протокол_пересмотра_нормы — Пересмотр нормы

**Уровень:** L3–L4

**Для чего:** Осознанное изменение старого правила, которое больше не работает. Выход из `#ловушка_универсальная_маска`, `#ловушка_стальной_зубр`.

**Алгоритм:**
1. Найти правило («Я должен всегда…»).
2. Отделить смысл от способа внедрения.
3. Переформулировать в здоровую взрослую версию.
4. Проверить на `ΔI`.
5. Внедрять микро-дозами.

**Связи (локальные):**
- `#ловушка_универсальная_маска` — закрывает
- `#ловушка_стальной_зубр` — закрывает

---

## 14. #метод_чистый_выход — Чистый выход

**Уровень:** L1–L3

**Для чего:** Завершение контакта без потери себя. Выход из отношений, которые стали токсичными.

**Алгоритм:**
1. Замерить `α` до и после контакта.
2. Вычислить `K_разр`.
3. Если `K > 5` — выйти.
4. Выход — без разрушения, но с фиксацией.

**Связи (локальные):**
- `#ловушка_фатальная_игра` — закрывает
- `#ловушка_контроль_через_уход` — закрывает

---

## 15. #метод_консолидация_или_исход — Консолидация или Исход

**Уровень:** L2–L3

**Для чего:** Стратегия выживания в токсичной среде. Выбор между укреплением позиции и выходом.

**Алгоритм:**
1. Оценить ресурс и среду.
2. Выбрать: одиночка (стелс), коалиция или бегство.
3. Если остаёшься — укреплять позиции.
4. Если уходишь — выходить без потерь.

**Связи (локальные):**
- `#ловушка_фатальная_игра` — закрывает
- `#ловушка_контроль_через_уход` — закрывает

---

## 16. #метод_калибровка_карты_мира — Калибровка карты мира

**Уровень:** L2–L3

**Для чего:** Сверка прогноза с реальностью. Выход из `#ловушка_розовые_очки`, `#ловушка_злой_мир`, `#ловушка_катастрофизация`.

**Алгоритм:**
1. Осознать: карта ≠ реальность.
2. Определить уровень `P_heat` и `T`.
3. Проверить направление достраивания.
4. Скорректировать через поиск подтверждающих и опровергающих фактов.

**Связи (локальные):**
- `#ловушка_розовые_очки` — закрывает
- `#ловушка_злой_мир` — закрывает
- `#ловушка_катастрофизация` — закрывает

---

## 17. #метод_проверка_пустоты — Проверка пустоты

**Уровень:** L2–L3

**Для чего:** Выход из `#ловушка_пустая_лодка`. Проверка, есть ли у действия намерение, или оно пустое.

**Алгоритм:**
1. Заметить импульс: «Он специально!»
2. Сделать паузу.
3. Спросить: «Есть ли здесь реальный субъект с намерением, или я достроил(а) капитана?»
4. Если пустота — признать и отпустить.

**Связи (локальные):**
- `#ловушка_пустая_лодка` — закрывает
- `#ловушка_богатое_воображение` — закрывает

---

## 18. #метод_микро_открытие — Микро-открытие

**Уровень:** L2–L3

**Для чего:** Постепенный выход из маски без катастрофы. Снижение `H_ext` и рост `H_self` через малые шаги.

**Алгоритм:**
1. Заметить момент, когда надеваешь маску («вот сейчас я защищаюсь»).
2. В следующий раз в похожей ситуации — на 5% меньше маски. Одна искренняя фраза вместо десяти подстроенных.
3. Проверить реакцию: катастрофа случилась или нет.
4. Если нет — закрепить. Если да — отступить на шаг назад и уменьшить процент.
5. Повторять, увеличивая долю подлинности постепенно.

**Ожидаемый результат:** `AG_flow` растёт на подлинных контактах, `P_heat` снижается, `H_self` стабилизируется.

**Ограничения:** Не работает при высоком `P_heat` (>8) — нужен сброс через `#метод_сброс_P_heat` или соматическую работу.

**Связи (локальные):**
- `#ловушка_универсальная_маска` — закрывает
- `#ловушка_принцессы_не_пукают` — закрывает
- `#механика_маска` — работает с механизмом
- `#переменная_H_self` — целевая переменная

---

## 19. #метод_выдержать_стыд — Выдержать стыд

**Уровень:** L1–L2

**Для чего:** Интеграция диссоциированного стыда. Перевод стыда из телесного застоя в переживаемый опыт.

**Алгоритм:**
1. Найти стыд в теле. Где он живёт? (диафрагма, горло, живот, лицо).
2. Не отвлекаться, не убегать, не замораживать. Присутствовать с ощущением.
3. Дышать в точку застоя. Медленно, без форсировки.
4. Дать себе разрешение: «Мне стыдно, и это нормально. Это не убивает».
5. Дождаться, пока интенсивность снизится на 20–30%. Не ждать полного исчезновения.

**Ожидаемый результат:** `Som` снижается, диссоциация уменьшается, стыд становится переживаемым, а не парализующим.

**Ограничения:** При травматическом стыде (`Som` > 9) — только с терапевтом. Не применять в изоляции без поддерживающего контакта.

**Связи (локальные):**
- `#ловушка_принцессы_не_пукают` — закрывает
- `#механика_диссоциация` — работает с механизмом
- `#механика_соматический_ключ` — точка входа
- `#эмоция_стыд` — целевая эмоция
- `#переменная_Som` — телесный маркер

---

## 20. #метод_разделение_мысли_и_действия — Разделение мысли и действия

**Уровень:** L2–L3

**Для чего:** Преодоление разрыва между L3 (понимание) и L0–L1 (поведение). «Всё знаю, но делаю наоборот».

**Алгоритм:**
1. Назвать разрыв прямо: «Я понимаю X, но делаю Y. Это не лицемерие — это разъединение уровней».
2. Найти соматический маркер момента автоматизма (где в теле срабатывает триггер маски?).
3. В момент срабатывания — вставить паузу на 3 секунды. Не действовать автоматически.
4. За эти 3 секунды задать вопрос: «Что я хочу СЕЙЧАС — маска или я?»
5. Выбрать, даже если выбор маленький. Зафиксировать результат.

**Ожидаемый результат:** Начало построения моста между неокортексом и рептильным мозгом. Постепенная автоматизация нового поведения.

**Ограничения:** Требует повторения 20–30 раз для формирования новой нейронной связи. Одного раза недостаточно.

**Связи (локальные):**
- `#ловушка_универсальная_маска` — закрывает
- `#механика_маска` — работает с автоматизмом
- `#переменная_α` — возвращает авторство
- `#аксиома_двусторонняя_связь` — основание

---

## Связи с файлами

| Файл | Что связывает |
|------|---------------|
| `02_VARIABLES.md` | Все методы работают с переменными |
| `04_TRAPS.md` | Каждый метод — выход из ловушки |
| `05_CHILDHOOD_RULES.md` | Супер-правила → методы |
| `07_PROTOCOLS.md` | Методы — часть протоколов |

---

**Корневой тег:** `#БО840`
